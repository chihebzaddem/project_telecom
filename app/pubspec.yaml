name: app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.7
  flutter_map: ^6.1.0
  latlong2: ^0.9.1
  geolocator: ^10.1.0
  permission_handler: ^11.0.0
  provider: ^6.1.1 
  url_launcher: ^6.2.5
  flutter_map_marker_popup: ^6.1.2

  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/logo.svg
    - assets/site.svg
    - assets/azimu.svg
    - assets/data/sites_2g.json
    - assets/data/sites_3g.json
    - assets/data/sites_4g.json